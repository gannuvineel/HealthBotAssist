  var age = Number(user.age)
  temp.correct = false
  if (!isNaN(age)) {
    temp.correct = true
  }
  if (age > 0 && age < 100) {
    temp.correct = true
  }

  if (temp['skill-choice-ret-k4rxw9rv0f'] == "yes")
  {
    temp.v = "You successfully selected yes"
  }